import { Button } from '../button';
export declare class InventoryButton extends Button {
    private worldEvents;
    private showInventory;
    constructor(scene: any, state: any, size: any, data: any, callback: any);
    toggleInventory: () => void;
}
