import { Button } from '../button';
export declare class SelectButton extends Button {
    constructor(scene: any, state: any, size: any, callback: any);
    enter: () => void;
    exit: () => void;
}
